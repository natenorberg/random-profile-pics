import * as React from 'react';

const ProfilePic = ({settings}) => (
  <div
    id="bigProfilePic"
    style={{width: 150, height: 150, borderRadius: '100%', background: '#888', margin: 16}}
  />
);

export default ProfilePic;
